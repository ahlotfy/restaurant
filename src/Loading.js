import React from "react";

const Loading = () => {
  return (
    <div
      style={{
        minHeight: "100vh",
        width: "100%",
        textAlign: "center",
        background: "black",
        color: "white",
        fontSize: "20px",
      }}
    >
      Loading...
    </div>
  );
};

export default Loading;
